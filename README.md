---
layout: default.html
title: README
---
# ArcLyn website

## running the website
- use `npm i` to install dependencies
- use `npm run start` to build + serve 
- use `npm run clean` to remove `_site`